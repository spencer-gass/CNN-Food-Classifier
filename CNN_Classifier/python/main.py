from trainers import *
from attributeModelEvaluators import *

if __name__ == '__main__':
    print()
    # food_or_not_trainer(lr=1e-3, epochs=10, batch_size=20, continue_training=False)
    # food_101_trainer(lr=1e-3, epochs=10, batch_size=20, continue_training=False)
    # good_for_dinner_trainer(lr=1e-4, epochs=80, batch_size=20, continue_training=True)
    # good_for_lunch_trainer(lr=1e-4, epochs=40, batch_size=20, continue_training=True)
    # good_for_lunch_or_dinner_trainer(lr=1e-4, epochs=20, batch_size=20, continue_training=True)
    # is_expensive_trainer(lr=1e-4, epochs=25, batch_size=20, continue_training=True)
    # has_alcohol_trainer(lr=1e-4, epochs=20, batch_size=20, continue_training=True)
    # outdoor_seating_trainer(lr=1e-3, epochs=25, batch_size=20, continue_training=False)
    #good_for_kids_trainer(lr=1e-3, epochs=16, batch_size=20, continue_training=False)

    # goodForLunchModelEvaluator()
    # goodForDinnerModelEvaluator()
    # isExpensiveModelEvaluator()
    # hasAlcoholModelEvaluator()
    # outdoorSeatingModelEvaluator()
    # goodForKidsModelEvaluator()